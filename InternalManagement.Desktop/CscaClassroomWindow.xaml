<Window x:Class="InternalManagement.Desktop.CscaClassroomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:InternalManagement.Desktop"
        Title="Danh mục phòng học CSCA"
        Icon="Assets/app-icon.ico"
        Width="980" Height="620" MinWidth="760" MinHeight="480"
        WindowStartupLocation="CenterOwner" Background="#F8FAFC" FontFamily="Segoe UI"
        Loaded="Window_Loaded">
    <Window.Resources>
        <Style x:Key="ToolButton" TargetType="Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="Padding" Value="14,0" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#CBD5E1" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
        </Style>
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Background" Value="#4F46E5" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#4F46E5" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="DangerButton" TargetType="Button" BasedOn="{StaticResource ToolButton}">
            <Setter Property="Foreground" Value="#B91C1C" />
            <Setter Property="BorderBrush" Value="#FECACA" />
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F1F5F9" />
            <Setter Property="Foreground" Value="#475569" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Padding" Value="10,8" />
        </Style>
    </Window.Resources>

    <Grid Margin="22">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="DANH MỤC PHÒNG HỌC" FontSize="20" FontWeight="Bold" Foreground="#0F172A" />
            <TextBlock Text="Chỉ các phòng đang hoạt động mới được chọn cho lịch học. Phòng đã phát sinh lịch chỉ có thể ngừng dùng, không thể xóa." Foreground="#64748B" Margin="0,5,0,0" />
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,10">
            <Button Content="+ Thêm phòng" Style="{StaticResource PrimaryButton}" Click="AddClassroom_Click" />
            <Button Content="✎ Sửa phòng" Style="{StaticResource ToolButton}" Click="EditClassroom_Click" />
            <Button Content="Xóa phòng" Style="{StaticResource DangerButton}" Click="RemoveClassroom_Click" />
            <Button Content="↻ Tải lại" Style="{StaticResource ToolButton}" Click="Refresh_Click" />
        </StackPanel>
        <DataGrid Grid.Row="2" x:Name="ClassroomsDataGrid" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False"
                  RowHeight="44" HeadersVisibility="Column" GridLinesVisibility="Horizontal" MouseDoubleClick="ClassroomsDataGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="MÃ PHÒNG" Binding="{Binding Code}" Width="140" />
                <DataGridTextColumn Header="TÊN PHÒNG" Binding="{Binding Name}" Width="1.5*" />
                <DataGridTextColumn Header="SỨC CHỨA" Binding="{Binding CapacityLabel}" Width="110" />
                <DataGridTextColumn Header="VỊ TRÍ" Binding="{Binding Location}" Width="1.6*" />
                <DataGridTextColumn Header="TRẠNG THÁI" Binding="{Binding StatusLabel}" Width="150" />
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="3" x:Name="StatusText" Foreground="#64748B" FontSize="12" Margin="2,10,0,0" />

        <!-- SLEEK MODAL BUSY OVERLAY -->
        <Border x:Name="ClassroomBusyOverlay" Grid.RowSpan="4" Background="#66090E1D" Visibility="Collapsed" Panel.ZIndex="100" Margin="-22">
            <Border Background="White" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="16" Padding="28,24" HorizontalAlignment="Center" VerticalAlignment="Center" MinWidth="280" MaxWidth="380" Effect="{StaticResource FloatingShadow}">
                <StackPanel HorizontalAlignment="Center">
                    <!-- Animated Dual-Orbit Spinner with Brand Badge (Mẫu 3) -->
                    <local:MolyDualOrbitSpinner Width="52" Height="52" HorizontalAlignment="Center" Margin="0,0,0,14" />
                    <TextBlock x:Name="ClassroomBusyText" Text="Đang xử lý dữ liệu..." HorizontalAlignment="Center" FontSize="14" FontWeight="Bold" Foreground="#0F172A" Margin="0,0,0,4" />
                    <TextBlock Text="Vui lòng đợi trong giây lát..." HorizontalAlignment="Center" FontSize="11.5" Foreground="#64748B" Margin="0,0,0,14" />
                    <ProgressBar IsIndeterminate="True" Width="200" Height="3.5" Style="{StaticResource ModernProgressBarStyle}" />
                </StackPanel>
            </Border>
        </Border>
    </Grid>
</Window>
